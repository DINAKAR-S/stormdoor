"""Reproducible proof for the claims in the README.

Everything runs against the local `echo` provider, so the harness needs no API
key, no network and no money, and anyone who clones the repo can regenerate the
numbers on their own machine.
"""
